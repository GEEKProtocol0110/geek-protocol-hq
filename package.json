{
  "name": "geek-protocol-hq",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "security:check": "node scripts/security-check.mjs",
    "verify": "npm test && npm run security:check"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@dfns/kaspa-wasm": "0.14.1"
  }
}
